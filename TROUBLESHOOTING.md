# 🔧 Усунення проблем

Типові проблеми та їх рішення для Voteku Test System.

---

## 🤖 Проблеми з Telegram Bot

### ❌ Bot не відповідає на команди

**Симптоми:**
- Надсилаєте `/start`, але нічого не відбувається
- Bot онлайн, але не реагує

**Можливі причини та рішення:**

1. **Невірний токен**
   ```bash
   # Перевірте .env файл
   cat .env

   # Переконайтеся, що TELEGRAM_BOT_TOKEN правильний
   # Формат: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

2. **Сервер не запущений**
   ```bash
   # Перевірте, чи працює backend
   npm run dev

   # Очікуйте повідомлення:
   # ✅ Telegram Bot запущен в режиме Long Polling
   ```

3. **Помилка polling**
   ```bash
   # Якщо бачите помилку "409 Conflict: terminated by other getUpdates"
   # Це означає, що бот запущений в іншому місці

   # Рішення: зупиніть всі інші екземпляри бота
   # Або скиньте webhook:
   curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/deleteWebhook
   ```

---

### ❌ Bot відповідає, але не зберігає дані

**Симптоми:**
- Можна розпочати тест
- Після введення імені нічого не відбувається
- В Admin Panel немає результатів

**Рішення:**

1. **Перевірте базу даних**
   ```bash
   # Переконайтеся, що файл існує
   ls -la voteku_test.db

   # Якщо файлу немає:
   npm run setup
   ```

2. **Перевірте логи сервера**
   ```bash
   # В терміналі де запущений backend шукайте помилки
   # Помилки бази даних виглядають так:
   # ❌ Ошибка создания сессии: ...
   ```

3. **Права доступу до файлу БД**
   ```bash
   # macOS / Linux
   chmod 644 voteku_test.db
   ```

---

### ❌ Bot завис на певному питанні

**Симптоми:**
- Відповіли на питання, але наступне не приходить
- Кнопки не працюють

**Рішення:**

1. **Перезапустіть тест**
   ```bash
   # В SQLite видаліть незавершену сессію
   sqlite3 voteku_test.db

   DELETE FROM user_states WHERE telegram_id = ВАШ_TELEGRAM_ID;
   DELETE FROM test_sessions WHERE telegram_id = ВАШ_TELEGRAM_ID AND status = 'in_progress';

   .exit

   # Потім в Telegram знову: /start
   ```

2. **Перевірте логи**
   ```bash
   # Шукайте помилки типу:
   # ❌ Ошибка отправки вопроса
   # ❌ Ошибка сохранения ответа
   ```

---

## 💻 Проблеми з Admin Panel

### ❌ Admin Panel не відкривається (localhost:5173)

**Симптоми:**
- "This site can't be reached"
- "Connection refused"

**Рішення:**

1. **Перевірте, чи запущений dev server**
   ```bash
   cd admin
   npm run dev

   # Очікуйте:
   # ➜  Local:   http://localhost:5173/
   ```

2. **Порт зайнятий**
   ```bash
   # Якщо бачите помилку "Port 5173 is already in use"

   # Windows
   netstat -ano | findstr :5173
   taskkill /PID <PID> /F

   # macOS / Linux
   lsof -ti:5173 | xargs kill -9
   ```

3. **Залежності не встановлені**
   ```bash
   cd admin
   npm install
   npm run dev
   ```

---

### ❌ "Failed to fetch" помилка в Admin Panel

**Симптоми:**
- Admin Panel відкривається, але показує помилку
- "Помилка завантаження результатів"

**Рішення:**

1. **Backend не запущений**
   ```bash
   # Переконайтеся, що backend працює на порту 3001
   npm run dev

   # Перевірте:
   curl http://localhost:3001/api/health

   # Очікувана відповідь:
   # {"status":"ok","timestamp":"..."}
   ```

2. **CORS помилка**
   ```bash
   # Перевірте логи backend на наявність:
   # Access to fetch at 'http://localhost:3001/api/results' has been blocked by CORS policy

   # Переконайтеся, що в server/index.js є:
   # app.use(cors());
   ```

3. **Невірний API URL**
   ```javascript
   // Перевірте admin/src/services/api.js
   // В режимі development має бути:
   const API_BASE_URL = 'http://localhost:3001/api';
   ```

---

## 💾 Проблеми з базою даних

### ❌ "Database is locked"

**Симптоми:**
- Помилка при збереженні результатів
- `SQLITE_BUSY: database is locked`

**Рішення:**

1. **Закрийте всі підключення**
   ```bash
   # Зупиніть всі екземпляри сервера
   # Ctrl+C в усіх терміналах

   # Видаліть lock файл
   rm voteku_test.db-journal
   rm voteku_test.db-wal
   ```

2. **Перезапустіть сервер**
   ```bash
   npm run dev
   ```

---

### ❌ База даних втратила дані після рестарту

**Симптоми:**
- Після рестарту комп'ютера всі результати зникли

**Це нормально для локальної розробки!**

Для збереження даних на production (Render.com):
- Переконайтеся, що налаштований **Persistent Disk**
- Перевірте Mount Path: `/opt/render/project/src`

---

## 🚀 Проблеми з деплоєм на Render

### ❌ Build Failed на Render

**Симптоми:**
- Деплой зупинився на етапі Build
- Помилка в логах

**Рішення:**

1. **Перевірте Build Command**
   ```bash
   # Має бути:
   npm install && cd admin && npm install && npm run build && cd ..
   ```

2. **Перевірте package.json**
   ```bash
   # Переконайтеся, що файли існують:
   # - /package.json
   # - /admin/package.json
   ```

3. **Очистіть build cache на Render**
   - Settings → Clear build cache → Manual Deploy

---

### ❌ Bot не відповідає на Render (Production)

**Симптоми:**
- Локально все працює
- На Render bot не відповідає

**Рішення:**

1. **Перевірте змінні оточення**
   - Переконайтеся, що `TELEGRAM_BOT_TOKEN` встановлений
   - Перевірте `WEBHOOK_URL` - має співпадати з URL сервісу

2. **Перевірте логи**
   ```
   Render Dashboard → Logs

   Шукайте:
   ✅ Telegram Bot запущен в режиме Webhook
   ```

3. **Перевірте webhook**
   ```bash
   curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo

   # Очікувана відповідь:
   {
     "url": "https://your-app.onrender.com/bot<TOKEN>",
     "has_custom_certificate": false,
     "pending_update_count": 0
   }
   ```

4. **Скиньте webhook**
   ```bash
   curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/deleteWebhook

   # Перезапустіть сервіс на Render
   # Render Dashboard → Manual Deploy → Deploy latest commit
   ```

---

### ❌ База даних порожня після рестарту на Render

**Симптоми:**
- Після рестарту сервісу всі дані зникли

**Рішення:**

**ВАЖЛИВО:** Переконайтеся, що налаштований Persistent Disk!

1. **Перевірте Disk в Render Dashboard:**
   - Settings → Disks
   - Name: `voteku-db`
   - Mount Path: `/opt/render/project/src`
   - Size: 1 GB

2. **Перевірте шлях до БД:**
   ```javascript
   // server/database/db.js
   const DB_PATH = process.env.DB_PATH || join(dirname(dirname(__dirname)), 'voteku_test.db');
   ```

3. **Додайте в .env на Render:**
   ```
   DB_PATH=/opt/render/project/src/voteku_test.db
   ```

---

## 📊 Проблеми з даними

### ❌ Користувач пройшов тест двічі

**Симптоми:**
- В базі даних 2 записи від одного користувача

**Причина:**
- Захист від повторного проходження працює по `telegram_id`
- Якщо в БД є тільки `in_progress` сесія, користувач може почати нову

**Рішення:**

Видаліть дублікати:

```bash
sqlite3 voteku_test.db

# Перегляньте дублікати
SELECT telegram_id, COUNT(*) FROM test_sessions GROUP BY telegram_id HAVING COUNT(*) > 1;

# Видаліть старі незавершені
DELETE FROM test_sessions WHERE status = 'in_progress' AND telegram_id = <ID>;

.exit
```

---

## 🔍 Корисні команди для діагностики

### Перевірка стану системи

```bash
# Перевірка бази даних
sqlite3 voteku_test.db "SELECT COUNT(*) as total FROM test_sessions;"

# Перевірка backend API
curl http://localhost:3001/api/health

# Перевірка Telegram Bot (на Render)
curl https://api.telegram.org/bot<ТОКЕН>/getMe

# Перевірка webhook (на Render)
curl https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo
```

### Очистка всіх даних

```bash
# УВАГА: Це видалить ВСІ результати тестування!

# Видалити базу даних
rm voteku_test.db

# Переініціалізувати
npm run setup
```

---

## 📞 Все ще не працює?

1. **Перевірте:**
   - Node.js версія 18+ (`node --version`)
   - Всі залежності встановлені (`npm install`)
   - Backend запущений (`npm run dev`)
   - Admin запущений (`cd admin && npm run dev`)

2. **Перегляньте логи:**
   - Backend термінал
   - Admin Panel термінал
   - Render Dashboard → Logs (для production)

3. **Створіть issue:**
   - Опишіть проблему
   - Додайте логи помилок
   - Вкажіть версію Node.js

---

**Успішного налагодження! 🐛→✅**
