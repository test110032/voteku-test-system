# 🚀 Полное руководство по деплою на Render.com

Пошаговая инструкция по развертыванию Voteku Test System на Render.com с использованием SQLite базы данных.

## 📋 Требования

- ✅ Аккаунт на [GitHub](https://github.com)
- ✅ Аккаунт на [Render.com](https://render.com) (бесплатно, без кредитной карты)
- ✅ Telegram Bot Token от [@BotFather](https://t.me/BotFather)

---

## Шаг 1: Создание Telegram Bot

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Введите название бота (например: `Voteku Test Bot`)
4. Введите username бота (должен заканчиваться на `bot`, например: `voteku_test_bot`)
5. **Сохраните токен**, который предоставит BotFather (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## Шаг 2: Загрузка кода на GitHub

### 2.1 Создайте новый репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Название: `voteku-test-system`
4. Сделайте репозиторий **Public** или **Private** (оба работают с Render)
5. Нажмите "Create repository"

### 2.2 Загрузите код

В терминале в папке проекта:

```bash
# Инициализация Git (если еще не сделано)
git init
git add .
git commit -m "Initial commit: Voteku Test System"

# Добавление remote и push
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/voteku-test-system.git
git push -u origin main
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

---

## Шаг 3: Деплой на Render.com

### 3.1 Зарегистрируйтесь на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите "Get Started for Free"
3. Зарегистрируйтесь через GitHub (рекомендуется)

### 3.2 Создайте Web Service

1. На дашборде Render нажмите **"New +"** → **"Web Service"**
2. Выберите "Build and deploy from a Git repository"
3. Нажмите "Next"

### 3.3 Подключите GitHub репозиторий

1. Найдите ваш репозиторий `voteku-test-system`
2. Нажмите "Connect"

### 3.4 Настройте сервис

Заполните форму:

**Basic Settings:**
- **Name**: `voteku-test-system` (или любое другое название)
- **Region**: `Frankfurt (EU Central)` (ближайший к Украине)
- **Branch**: `main`
- **Root Directory**: (оставьте пустым)
- **Runtime**: `Node`

**Build Settings:**

Render может автоматически обнаружить настройки из `render.yaml`, но можно указать вручную:

- **Build Command**:
  ```bash
  npm install && npm install --prefix admin && npm run build --prefix admin
  ```

- **Start Command**:
  ```bash
  npm start
  ```

**Instance Type:**
- Выберите **"Free"** ($0 в месяц)

### 3.5 Добавьте переменные окружения

Прокрутите до раздела **"Environment Variables"** и добавьте:

| Key | Value | Описание |
|-----|-------|----------|
| `NODE_ENV` | `production` | Режим работы приложения |
| `PORT` | `3001` | Порт сервера (необязательно, Render использует свой) |
| `TELEGRAM_BOT_TOKEN` | `ваш_токен_от_BotFather` | Токен Telegram бота |
| `WEBHOOK_URL` | `https://voteku-test-system.onrender.com` | URL для webhook |

⚠️ **ВАЖНО**:
- В `WEBHOOK_URL` замените `voteku-test-system` на название, которое вы указали в **Name**!
- Если название содержит пробелы, Render заменит их на дефисы

### 3.6 Создайте Web Service

Нажмите большую синюю кнопку **"Create Web Service"** внизу страницы.

---

## Шаг 4: Ожидание деплоя

Render начнет автоматический деплой:

1. ⏱ **Сборка** (Build) - 2-5 минут
   - Клонирование репозитория
   - Установка зависимостей (`npm install`)
   - Сборка админ-панели (`npm run build --prefix admin`)

2. 🚀 **Деплой** - 1-2 минуты
   - Запуск приложения
   - Инициализация базы данных
   - Настройка Telegram webhook

Вы увидите логи в реальном времени. Ожидайте сообщение:

```
🚀 Запуск Voteku Test System...
✅ Подключение к SQLite успешно: /opt/render/project/src/voteku_test.db
✅ База данных инициализирована
✅ Telegram Bot запущен в режиме Webhook
✅ Сервер запущен на порту 3001
📍 Режим: production
🌐 API: http://localhost:3001/api
✨ Система готова к работе!
==> Your service is live 🎉
```

---

## Шаг 5: Проверка работы

### 5.1 Проверьте Admin Panel

Откройте в браузере: `https://voteku-test-system.onrender.com`

(Замените `voteku-test-system` на ваше название сервиса)

Вы должны увидеть страницу с заголовком "🎓 Voteku Test System - Админ панель"

### 5.2 Проверьте API

Откройте: `https://voteku-test-system.onrender.com/api/health`

Должен вернуться JSON:
```json
{
  "status": "ok",
  "database": "connected",
  "timestamp": "2024-01-15T10:30:00Z"
}
```

### 5.3 Проверьте Telegram Bot

1. Откройте Telegram
2. Найдите своего бота (username, который вы создали в BotFather)
3. Нажмите `/start`
4. Введите свое имя и фамилию
5. Пройдите тест!

### 5.4 Проверьте результаты в Admin Panel

После прохождения теста:
1. Обновите страницу админ-панели
2. Вы должны увидеть свой результат в списке
3. Нажмите на результат для детального просмотра всех ответов

---

## Шаг 6: Настройка автоматического деплоя

Render автоматически настраивает CI/CD:

### Автоматический редеплой при изменениях

Каждый раз когда вы делаете `git push` в ветку `main`, Render автоматически:
1. Обнаруживает изменения
2. Пересобирает приложение
3. Деплоит новую версию

### Пример: обновление вопросов

```bash
# Отредактируйте questions.json
nano questions.json

# Закоммитьте изменения
git add questions.json
git commit -m "Update test questions"
git push

# Render автоматически задеплоит изменения
```

---

## Шаг 7: Мониторинг и логи

### Просмотр логов на Render

1. Откройте ваш Web Service на Render
2. Перейдите во вкладку **"Logs"**
3. Здесь вы увидите все события:
   - Запуск сервера
   - Запросы к API
   - Сообщения от Telegram Bot
   - Ошибки (если есть)

### Полезные логи

**Успешная инициализация:**
```
✅ Подключение к SQLite успешно
✅ База данных инициализирована
✅ Telegram Bot запущен в режиме Webhook
```

**Новый пользователь начал тест:**
```
👤 Новый пользователь: Иван Петренко (ID: 123456789)
📝 Начат новый тест
```

**Тест завершен:**
```
✅ Тест завершен. Результат: 25/30
```

---

## 🎉 Готово!

Ваша система тестирования запущена и доступна через интернет 24/7!

### Что дальше?

- ✅ Отправьте ссылку на бота сотрудникам
- ✅ Открывайте admin panel для просмотра результатов
- ✅ Добавляйте новые вопросы в `questions.json` и делайте `git push`
- ✅ Мониторьте логи через Dashboard Render

---

## ⚙️ Дополнительные настройки

### Настройка домена (опционально)

Render позволяет подключить свой домен:

1. Перейдите в **Settings** → **Custom Domains**
2. Добавьте свой домен
3. Настройте DNS записи (Render предоставит инструкции)
4. Обновите `WEBHOOK_URL` на новый домен

### Масштабирование (платные планы)

Если нужно больше ресурсов:
- **Starter** ($7/месяц): больше памяти, без засыпания
- **Standard** ($25/месяц): еще больше ресурсов, автомасштабирование

---

## ⚠️ Ограничения бесплатного плана Render

### Особенности Free Tier:

- 🔋 **Засыпание**: Сервис засыпает после **15 минут** бездействия
- ⏰ **Холодный старт**: Первый запрос после сна занимает **~30 секунд**
- 📅 **Лимит времени**: **750 часов/месяц** (достаточно для 1 проекта)
- 💾 **Диск**: SQLite база данных сохраняется на файловой системе
- 🔄 **Перезапуски**: Могут происходить при обслуживании

### Как уникнуть засыпания (опционально)

Используйте бесплатный сервис [UptimeRobot](https://uptimerobot.com) или [Cron-job.org](https://cron-job.org):

**UptimeRobot:**
1. Зарегистрируйтесь на UptimeRobot
2. Добавьте монитор:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: Voteku Test System
   - **URL**: `https://voteku-test-system.onrender.com/api/health`
   - **Monitoring Interval**: Every 5 minutes
3. UptimeRobot будет "будить" ваш сервис каждые 5 минут

**Альтернатива - Cron-job.org:**
1. Создайте бесплатный аккаунт
2. Добавьте новое задание:
   - **URL**: `https://voteku-test-system.onrender.com/api/health`
   - **Interval**: Every 5 minutes
   - **Method**: GET

---

## 🐛 Устранение проблем

### Проблема: Build failed - "vite: not found"

**Причина:** Vite не установлен как production зависимость

**Решение:**
```bash
# В файле admin/package.json переместите vite в dependencies
# Уже исправлено в текущей версии
git pull
```

### Проблема: Bot не отвечает

**Решение:**
1. Проверьте логи на Render
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
3. Проверьте, что `WEBHOOK_URL` соответствует URL вашего сервиса
4. Попробуйте вручную установить webhook:
   ```bash
   curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://voteku-test-system.onrender.com/bot<YOUR_BOT_TOKEN>
   ```

### Проблема: База данных пустая после рестарта

**Причина:** SQLite работает на файловой системе Render. Данные сохраняются между деплоями.

**Решение:**
- Данные должны сохраняться автоматически
- Если данные потерялись, возможно произошел редеплой или перезапуск сервиса
- Рассмотрите переход на PostgreSQL для более надежного хранения (см. render.yaml - уже настроен)

### Проблема: 502 Bad Gateway

**Причина:** Сервис засыпает или перезапускается

**Решение:**
1. Подождите 30-60 секунд (холодный старт)
2. Обновите страницу
3. Настройте UptimeRobot для предотвращения засыпания

### Проблема: "Your service could not be deployed because the Git repository is empty"

**Причина:** Репозиторий не содержит коммитов

**Решение:**
```bash
git add .
git commit -m "Initial commit"
git push -u origin main
```

### Проблема: Environment variable not set

**Решение:**
1. Перейдите в **Settings** → **Environment** на Render
2. Добавьте отсутствующую переменную
3. Нажмите "Save Changes"
4. Render автоматически перезапустит сервис

---

## 🔄 Процесс обновления приложения

### Обновление кода

```bash
# 1. Внесите изменения в код
nano server/index.js

# 2. Закоммитьте
git add .
git commit -m "Update server configuration"

# 3. Запушьте
git push

# 4. Render автоматически задеплоит изменения
```

### Обновление вопросов

```bash
# 1. Отредактируйте questions.json
nano questions.json

# 2. Закоммитьте и запушьте
git add questions.json
git commit -m "Add new questions"
git push
```

### Откат к предыдущей версии

На Render Dashboard:
1. Перейдите во вкладку **"Events"**
2. Найдите нужный деплой
3. Нажмите **"Redeploy"** на старой версии

---

## 📊 Мониторинг производительности

### Метрики Render

Render предоставляет:
- **CPU Usage**: использование процессора
- **Memory Usage**: использование памяти
- **Response Time**: время ответа
- **HTTP Status Codes**: статистика ответов

Доступно во вкладке **"Metrics"**

### Проверка здоровья приложения

```bash
# Health check endpoint
curl https://voteku-test-system.onrender.com/api/health

# Список результатов
curl https://voteku-test-system.onrender.com/api/results
```

---

## 🔐 Безопасность в Production

### Рекомендации:

1. **Храните токены в Environment Variables**
   - Никогда не коммитьте `.env` файл в Git
   - Используйте `.env.example` как шаблон

2. **Используйте HTTPS**
   - Render автоматически предоставляет SSL сертификат
   - Все запросы идут через HTTPS

3. **Webhook Security**
   - Токен бота включен в URL webhook для безопасности
   - Формат: `/bot<TOKEN>`

4. **CORS настроен**
   - API доступен с любого домена (можно ограничить при необходимости)

5. **Защита от SQL инъекций**
   - Используются prepared statements в SQLite

---

## 📞 Поддержка

### Полезные ресурсы:

- [Документация Render](https://render.com/docs)
- [Node.js на Render](https://render.com/docs/deploy-node-express-app)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [README проекта](README.md)
- [Troubleshooting](TROUBLESHOOTING.md)

### Если возникли проблемы:

1. Проверьте логи на Render
2. Проверьте переменные окружения
3. Убедитесь, что код последней версии на GitHub
4. Посмотрите раздел "Устранение проблем" выше

---

## ✅ Чеклист успешного деплоя

- [ ] Telegram Bot создан, токен получен
- [ ] Код загружен на GitHub
- [ ] Web Service создан на Render
- [ ] Все переменные окружения настроены
- [ ] Build прошел успешно
- [ ] Сервис запущен (статус "Live")
- [ ] Admin Panel открывается
- [ ] API health check возвращает OK
- [ ] Telegram Bot отвечает на /start
- [ ] Можно пройти тест через бота
- [ ] Результаты отображаются в Admin Panel

---

**Успешного деплоя! 🚀**

*Если у вас возникли вопросы или проблемы, проверьте логи на Render и документацию проекта.*
