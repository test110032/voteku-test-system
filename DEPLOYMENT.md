# 🚀 Гайд по деплою на Render.com

## Передумови

- ✅ Аккаунт на [GitHub](https://github.com)
- ✅ Аккаунт на [Render.com](https://render.com) (безкоштовно, без кредитної картки)
- ✅ Telegram Bot Token від [@BotFather](https://t.me/BotFather)

---

## Крок 1: Створення Telegram Bot

1. Відкрийте Telegram і знайдіть **@BotFather**
2. Надішліть команду `/newbot`
3. Введіть назву бота (наприклад: `Voteku Test Bot`)
4. Введіть username бота (має закінчуватися на `bot`, наприклад: `voteku_test_bot`)
5. **Збережіть токен**, який надасть BotFather (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## Крок 2: Завантаження коду на GitHub

### 2.1 Створіть новий репозиторій на GitHub

1. Перейдіть на [GitHub](https://github.com)
2. Натисніть "New repository"
3. Назва: `voteku-test-system`
4. Зробіть репозиторій **Public**
5. Натисніть "Create repository"

### 2.2 Завантажте код

У терміналі в папці проекту:

```bash
git init
git add .
git commit -m "Initial commit: Voteku Test System"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/voteku-test-system.git
git push -u origin main
```

**Замініть `YOUR_USERNAME` на ваш GitHub username!**

---

## Крок 3: Деплой на Render.com

### 3.1 Зареєструйтеся на Render

1. Перейдіть на [render.com](https://render.com)
2. Натисніть "Get Started for Free"
3. Зареєструйтеся через GitHub (рекомендовано)

### 3.2 Створіть Web Service

1. На дашборді Render натисніть **"New +"** → **"Web Service"**
2. Виберіть "Build and deploy from a Git repository"
3. Натисніть "Next"

### 3.3 Підключіть GitHub репозиторій

1. Знайдіть ваш репозиторій `voteku-test-system`
2. Натисніть "Connect"

### 3.4 Налаштуйте сервіс

Заповніть форму:

**Basic Settings:**
- **Name**: `voteku-test-system` (або будь-яка інша назва)
- **Region**: `Frankfurt (EU Central)` (найближча до України)
- **Branch**: `main`
- **Root Directory**: (залиште порожнім)
- **Runtime**: `Node`

**Build Settings:**
- **Build Command**:
  ```bash
  npm install && cd admin && npm install && npm run build && cd ..
  ```
- **Start Command**:
  ```bash
  npm start
  ```

**Instance Type:**
- Виберіть **"Free"** (0$ на місяць)

### 3.5 Додайте змінні оточення

Прокрутіть до розділу **"Environment Variables"** та додайте:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `PORT` | `3001` |
| `TELEGRAM_BOT_TOKEN` | `ваш_токен_від_BotFather` |
| `WEBHOOK_URL` | `https://voteku-test-system.onrender.com` |

⚠️ **ВАЖЛИВО**: В `WEBHOOK_URL` замініть `voteku-test-system` на назву, яку ви вказали в **Name**!

### 3.6 Налаштуйте Persistent Disk (для SQLite бази даних)

**Прокрутіть до розділу "Disks":**

1. Натисніть **"Add Disk"**
2. Заповніть:
   - **Name**: `voteku-db`
   - **Mount Path**: `/opt/render/project/src`
   - **Size**: `1 GB` (безкоштовно)
3. Натисніть "Save"

### 3.7 Створіть Web Service

Натисніть велику синю кнопку **"Create Web Service"** внизу сторінки.

---

## Крок 4: Очікування деплою

Render почне автоматичний деплой:

1. ⏱ **Збирання** (Build) - 2-5 хвилин
2. 🚀 **Деплой** - 1-2 хвилини

Ви побачите логи в реальному часі. Очікуйте повідомлення:

```
✅ Сервер запущен на порту 3001
✅ Telegram Bot запущен в режиме Webhook
✨ Система готова к работе!
```

---

## Крок 5: Перевірка роботи

### 5.1 Перевірте admin panel

Відкрийте в браузері: `https://voteku-test-system.onrender.com`

(Замініть `voteku-test-system` на вашу назву сервісу)

Ви повинні побачити заголовок "🎓 Voteku Test System - Адмін панель"

### 5.2 Перевірте Telegram Bot

1. Відкрийте Telegram
2. Знайдіть свого бота (username, який ви створили в BotFather)
3. Натисніть `/start`
4. Введіть своє ім'я
5. Пройдіть тест!

---

## Крок 6: Моніторинг та логи

### Перегляд логів на Render

1. Відкрийте ваш Web Service на Render
2. Перейдіть до вкладки **"Logs"**
3. Тут ви побачите всі події: помилки, запити, відповіді

### Перегляд бази даних

Щоб переглянути базу даних:

1. Перейдіть до вкладки **"Shell"** на Render
2. Виконайте команди:
   ```bash
   sqlite3 voteku_test.db
   SELECT * FROM test_sessions;
   .exit
   ```

---

## 🎉 Готово!

Ваша система тестування запущена і доступна через інтернет 24/7!

### Що далі?

- ✅ Надішліть посилання на бота співробітникам
- ✅ Відкривайте admin panel для перегляду результатів
- ✅ Додавайте нові питання в `questions.json` та робіть `git push`

---

## ⚠️ Обмеження безкоштовного плану Render

- 🔋 Сервіс засипає після **15 хвилин** бездіяльності
- ⏰ Перший запит після сну займе **~30 секунд**
- 📅 **750 годин/місяць** безкоштовної роботи (достатньо для 1 проекту)
- 💾 **1 GB** диску (достатньо для тисяч тестів)

### Як уникнути засипання (опціонально)

Використайте безкоштовний сервіс [UptimeRobot](https://uptimerobot.com):

1. Зареєструйтеся на UptimeRobot
2. Додайте монітор:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: Voteku Test System
   - **URL**: `https://voteku-test-system.onrender.com/api/health`
   - **Monitoring Interval**: Every 5 minutes

Це буде "будити" ваш сервіс кожні 5 хвилин.

---

## 🐛 Усунення проблем

### Проблема: Bot не відповідає

**Рішення:**
1. Перевірте логи на Render
2. Переконайтеся, що `TELEGRAM_BOT_TOKEN` правильний
3. Перевірте, що `WEBHOOK_URL` відповідає URL вашого сервісу

### Проблема: База даних порожня після рестарту

**Рішення:**
1. Переконайтеся, що **Persistent Disk** налаштований
2. Перевірте **Mount Path**: `/opt/render/project/src`
3. Перезапустіть сервіс

### Проблема: Build failed

**Рішення:**
1. Перевірте, чи всі файли завантажені на GitHub
2. Перевірте, чи `package.json` існує в корені проекту
3. Перегляньте логи помилок в розділі "Logs"

---

## 📞 Підтримка

Якщо виникли питання:
1. Перегляньте [README.md](README.md)
2. Перевірте логи на Render
3. Перевірте документацію [Render.com](https://render.com/docs)

Успішного деплою! 🚀
