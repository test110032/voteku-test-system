# 🎓 Voteku Test System

Система тестирования сотрудников с интерфейсом Telegram Bot и веб-панелью администратора.

## 📋 Особенности

- ✅ **Telegram Bot** для прохождения теста
- 📊 **Веб-панель администратора** для просмотра результатов
- 🎲 **Случайная генерация** 30 вопросов из банка 90 вопросов
- 💾 **SQLite база данных** для хранения результатов
- 🔒 **Защита от повторного прохождения** по Telegram ID
- 📱 **Адаптивный интерфейс**
- 🚀 **Готово к деплою** на Render.com (бесплатно)

## 🌐 Production

**Приложение развернуто и доступно по адресу:**
- 🔗 **URL**: https://voteku-test-system.onrender.com
- 🤖 **Telegram Bot**: [Ваш бот в Telegram]
- 📊 **Admin Panel**: https://voteku-test-system.onrender.com

## 🏗 Архитектура

```
voteku_test/
├── server/              # Backend (Node.js + Express)
│   ├── bot/            # Telegram Bot (node-telegram-bot-api)
│   ├── database/       # SQLite схема и подключение
│   ├── services/       # Бизнес-логика
│   │   ├── testService.js   # Генерация тестов
│   │   ├── resultService.js # Работа с результатами
│   │   └── stateService.js  # Управление сессиями
│   └── routes/         # API endpoints
├── admin/              # Frontend (React + Vite)
│   └── src/
│       ├── components/ # React компоненты
│       └── services/   # API клиент (Axios)
├── questions.json      # Банк вопросов (90 вопросов)
├── render.yaml        # Конфигурация для Render.com
└── .env.example       # Шаблон переменных окружения
```

## 🚀 Быстрый старт (локальная разработка)

### Требования

- Node.js 18+ установлен
- Telegram Bot Token (получите у [@BotFather](https://t.me/BotFather))

### Шаг 1: Клонирование и установка зависимостей

```bash
# Установка зависимостей backend
npm install

# Установка зависимостей admin panel
cd admin
npm install
cd ..
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
PORT=3001
NODE_ENV=development
DB_PATH=./voteku_test.db
```

### Шаг 3: Инициализация базы данных

```bash
npm run setup
```

### Шаг 4: Запуск в режиме разработки

**Запустите backend сервер:**
```bash
npm run dev
```

**В отдельном терминале запустите admin panel:**
```bash
cd admin
npm run dev
```

- Backend доступен на `http://localhost:3001`
- Admin Panel доступен на `http://localhost:5173`

## 🤖 Использование Telegram Bot

1. Найдите своего бота в Telegram
2. Нажмите `/start`
3. Введите свое Имя и Фамилию
4. Отвечайте на 30 вопросов, выбирая один вариант
5. Получите результат после завершения

**Защита от повторного прохождения:** Каждый пользователь (по Telegram ID) может пройти тест только один раз.

## 📊 Admin Panel

Веб-панель для просмотра результатов тестирования:

- **Главная страница**: Список всех пользователей, которые прошли тест
  - Имя пользователя
  - Результат (баллы/всего вопросов)
  - Процент правильных ответов
  - Дата и время прохождения

- **Детальный просмотр**: Все вопросы и ответы конкретного пользователя
  - Вопрос
  - Варианты ответов
  - Ответ пользователя
  - Правильный ответ
  - Статус (правильно/неправильно)

## 🌐 Деплой на Render.com

Подробная инструкция по деплою находится в [DEPLOYMENT.md](DEPLOYMENT.md)

### Краткая инструкция:

1. **Создайте репозиторий на GitHub** и запушьте код
2. **Зарегистрируйтесь на [Render.com](https://render.com)**
3. **Создайте Web Service** и подключите GitHub репозиторий
4. **Настройте переменные окружения:**
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `NODE_ENV=production`
   - `WEBHOOK_URL` - будет `https://your-app-name.onrender.com`
5. **Деплой** происходит автоматически!

**Важно:** Проект настроен для использования SQLite в production. Данные сохраняются в файловой системе Render.

## 📁 Структура базы данных

### `test_sessions`
Хранит сессии тестирования

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Первичный ключ |
| user_name | TEXT | Имя пользователя |
| telegram_id | INTEGER | Telegram ID (уникальный) |
| score | INTEGER | Набранные баллы |
| total_questions | INTEGER | Общее количество вопросов (30) |
| status | TEXT | in_progress / completed |
| started_at | DATETIME | Время начала |
| completed_at | DATETIME | Время завершения |

### `test_logs`
Детальные логи ответов

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Первичный ключ |
| session_id | INTEGER | Связь с test_sessions |
| question_id | INTEGER | ID вопроса |
| question_text | TEXT | Текст вопроса |
| options | TEXT | Варианты ответов (JSON) |
| user_answer_index | INTEGER | Индекс ответа пользователя |
| correct_answer_index | INTEGER | Индекс правильного ответа |
| is_correct | INTEGER | Правильный ли ответ (0/1) |
| answered_at | DATETIME | Время ответа |

## 🛠 API Endpoints

### `GET /api/health`
Health check endpoint

**Ответ:**
```json
{
  "status": "ok",
  "database": "connected",
  "timestamp": "2024-01-15T10:30:00Z"
}
```

### `GET /api/results`
Получить список всех завершенных тестов

**Ответ:**
```json
[
  {
    "id": 1,
    "user_name": "Иван Петренко",
    "telegram_id": 123456789,
    "score": 25,
    "total_questions": 30,
    "completed_at": "2024-01-15T10:30:00Z"
  }
]
```

### `GET /api/results/:sessionId`
Получить детальную информацию о конкретном тесте

**Ответ:**
```json
{
  "sessionInfo": {
    "id": 1,
    "user_name": "Иван Петренко",
    "score": 25,
    "total_questions": 30
  },
  "logs": [
    {
      "question_text": "Вопрос...",
      "options": ["Вариант А", "Вариант Б", "Вариант В"],
      "user_answer_index": 0,
      "correct_answer_index": 0,
      "is_correct": true
    }
  ]
}
```

## 🔧 Настройка

### Добавление новых вопросов

Редактируйте файл `questions.json`:

```json
{
  "id": 91,
  "question": "Ваш вопрос?",
  "options": [
    "Вариант А",
    "Вариант Б",
    "Вариант В"
  ],
  "correctAnswerIndex": 0
}
```

**После изменения вопросов:**
```bash
# Локально
npm run setup  # Переинициализирует БД

# Production (Render)
git add questions.json
git commit -m "Update questions"
git push  # Автоматический редеплой
```

### Изменение количества вопросов

В файле [server/services/testService.js](server/services/testService.js) измените параметр `count`:

```javascript
export const generateRandomTest = (count = 30) => { // Измените 30 на нужное количество
  // ...
}
```

## 🐛 Отладка

### Проверка базы данных

```bash
# Установите sqlite3
npm install -g sqlite3

# Откройте БД
sqlite3 voteku_test.db

# Просмотр таблиц
.tables

# Просмотр данных
SELECT * FROM test_sessions;
SELECT * FROM test_logs;

# Выход
.exit
```

### Проверка логов Render

1. Зайдите в Dashboard на Render.com
2. Выберите ваш сервис
3. Перейдите в раздел **Logs**
4. Просмотрите логи запуска и работы приложения

### Очистка данных тестов

```bash
# Локально
npm run setup

# Production - через SSH на Render или переразвертывание
```

## 📝 NPM Scripts

```bash
npm run dev      # Запуск в режиме разработки с hot-reload
npm start        # Production запуск
npm run setup    # Инициализация базы данных
```

## 🔒 Безопасность

- ✅ Telegram Bot Token хранится в переменных окружения
- ✅ CORS настроен для безопасного доступа к API
- ✅ Webhook использует секретный токен в URL
- ✅ Защита от SQL инъекций через prepared statements

## 📦 Технологии

**Backend:**
- Node.js + Express
- SQLite3
- node-telegram-bot-api
- dotenv для переменных окружения

**Frontend:**
- React 18
- React Router DOM
- Axios
- Vite для сборки

**Деплой:**
- Render.com (Web Service)
- GitHub для хранения кода

## 🚨 Важные замечания для Production

1. **База данных**: Используется SQLite. Данные сохраняются в файловой системе Render.
2. **Webhook**: При деплое на Render автоматически настраивается webhook для Telegram Bot.
3. **Переменные окружения**: Обязательно настройте `TELEGRAM_BOT_TOKEN` и `WEBHOOK_URL` в Render.
4. **Автоматический деплой**: При push в GitHub репозиторий Render автоматически пересобирает приложение.
5. **Логи**: Все логи доступны в Dashboard Render.

## 🆘 Поддержка

Если возникли вопросы или проблемы:

1. Проверьте логи сервера
2. Проверьте правильность `TELEGRAM_BOT_TOKEN`
3. Убедитесь, что база данных инициализирована (`npm run setup`)
4. Проверьте переменные окружения в Render
5. Посмотрите [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

## 📝 Лицензия

MIT

## 👥 Авторы

Разработано для Voteku

---

**🔗 Полезные ссылки:**
- [Детальная инструкция по деплою](DEPLOYMENT.md)
- [Руководство по началу работы](GETTING_STARTED.md)
- [Решение проблем](TROUBLESHOOTING.md)
- [История изменений](CHANGES.md)
