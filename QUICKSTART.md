# ⚡ Швидкий старт (Локальна розробка)

Інструкція для швидкого запуску проекту на вашому комп'ютері.

## 📋 Передумови

Встановіть на вашому комп'ютері:

- ✅ [Node.js 18+](https://nodejs.org/) (перевірте: `node --version`)
- ✅ [Git](https://git-scm.com/) (опціонально)

## 🚀 Крок 1: Створення Telegram Bot

1. Відкрийте Telegram
2. Знайдіть **@BotFather**
3. Надішліть: `/newbot`
4. Введіть назву: `Voteku Test Bot`
5. Введіть username: `voteku_test_bot` (або інший, що закінчується на `bot`)
6. **Збережіть токен!** (формат: `1234567890:ABCdefGHI...`)

## 🛠 Крок 2: Встановлення залежностей

Відкрийте термінал в папці проекту:

```bash
# Встановлення залежностей backend
npm install

# Встановлення залежностей frontend (admin panel)
cd admin
npm install
cd ..
```

## 🔑 Крок 3: Налаштування змінних оточення

Створіть файл `.env` в **корені проекту**:

```bash
# Windows (PowerShell)
Copy-Item .env.example .env

# macOS / Linux
cp .env.example .env
```

Відкрийте файл `.env` в редакторі і замініть:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_від_BotFather
PORT=3001
NODE_ENV=development
DB_PATH=./voteku_test.db
```

## 💾 Крок 4: Ініціалізація бази даних

```bash
npm run setup
```

Ви побачите:
```
🚀 Запуск инициализации базы данных...
✅ База данных инициализирована
✅ База данных успешно создана и готова к использованию!
```

## ▶️ Крок 5: Запуск сервера

**Відкрийте 2 термінали:**

### Термінал 1: Backend

```bash
npm run dev
```

Очікуйте повідомлення:
```
✅ Подключение к SQLite успешно
✅ База данных инициализирована
✅ Telegram Bot запущен в режиме Long Polling
✅ Сервер запущен на порту 3001
```

### Термінал 2: Admin Panel

```bash
cd admin
npm run dev
```

Очікуйте повідомлення:
```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:5173/
```

## ✅ Крок 6: Тестування

### 6.1 Тестування Telegram Bot

1. Відкрийте Telegram
2. Знайдіть свого бота (username з кроку 1)
3. Натисніть **"Start"** або надішліть `/start`
4. Введіть своє ім'я: `Тестовий Користувач`
5. Відповідайте на питання!

### 6.2 Перегляд результатів в Admin Panel

1. Відкрийте браузер: `http://localhost:5173`
2. Ви побачите список користувачів, які пройшли тест
3. Натисніть **"Детально"** для перегляду відповідей

## 🎯 Що далі?

### Додати нові питання

1. Відкрийте [questions.json](questions.json)
2. Додайте нове питання в кінець масиву:

```json
{
  "id": 91,
  "question": "Ваше нове питання?",
  "options": [
    "Варіант А",
    "Варіант Б",
    "Варіант В"
  ],
  "correctAnswerIndex": 0
}
```

3. Перезапустіть сервер (Ctrl+C → `npm run dev`)

### Перегляд бази даних

Встановіть SQLite командний інструмент:

```bash
npm install -g sqlite3
```

Відкрийте базу даних:

```bash
sqlite3 voteku_test.db

# Перегляд таблиць
.tables

# Перегляд сесій
SELECT * FROM test_sessions;

# Перегляд відповідей
SELECT * FROM test_logs;

# Вихід
.exit
```

## 🐛 Усунення проблем

### Проблема: "Cannot find module"

**Рішення:**
```bash
npm install
cd admin && npm install
```

### Проблема: Bot не відповідає

**Рішення:**
1. Перевірте файл `.env` - чи правильний токен?
2. Перевірте логи в терміналі backend
3. Переконайтеся, що backend працює

### Проблема: Admin Panel не відкривається

**Рішення:**
1. Перевірте, чи працює термінал 2 (admin)
2. Спробуйте: `http://localhost:5173`
3. Перевірте логи в терміналі

### Проблема: База даних порожня

**Рішення:**
```bash
npm run setup
```

## 📚 Корисні команди

```bash
# Перезапуск backend з автоматичним перезавантаженням
npm run dev

# Перезапуск admin panel
cd admin && npm run dev

# Переініціалізація бази даних (видалить всі дані!)
rm voteku_test.db
npm run setup

# Збірка admin panel для production
cd admin && npm run build

# Запуск в production режимі
npm start
```

## 🌐 Готові до деплою?

Коли все працює локально, перейдіть до [DEPLOYMENT.md](DEPLOYMENT.md) для інструкції з деплою на Render.com.

---

**Успішної розробки! 🚀**
